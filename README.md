# Vagrant Empty Vm Machine

# Object

Empty vagrant machine implement:
  - Docker
  - Docker Compose
  - Kubectl
  - Minikube
  - Ansible

# How start

```bash
run: `make up`
provisioning: `make provision`
destroy: `make rm`
```

# See help

linux: `make help`
